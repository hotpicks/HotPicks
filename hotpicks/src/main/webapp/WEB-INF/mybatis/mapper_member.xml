<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.kitri.hotpicks.member.dao.MemberDao">
 	
 	<select id="login" parameterType="memberDto" resultType="memberDto">
 		select userId, name
 		from member
 		where userId = #{userId}
 		and pass = #{pass}
 	</select>
 	
 	<insert id="join" parameterType="memberDto">
 		insert into member(userid, pass, name, age, gender, joinDate, sidoCode, sigunguCode, x, y, enable, profile)
		values(#{userId}, #{pass}, #{name}, #{age}, #{gender}, sysdate, #{sidoCode}, #{sigunguCode}, #{x}, #{y}, 1, #{profile})
 	</insert>
 	
 	<select id="idCheck" parameterType="String" resultType="int">
 		select count(*)
 		from member
 		where userId = #{userId}
 	</select>
 
 </mapper>